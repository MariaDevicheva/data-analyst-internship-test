# Music Analytics ETL Pipeline

**Python | Pandas | SQL | SQLite | Docker | ETL**

## Описание проекта

Проект представляет собой ETL-конвейер для извлечения, очистки, загрузки и анализа данных из нескольких разнородных источников.

В рамках проекта реализована обработка данных о клиентах, заказах, товарах, платежах и пользовательских событиях с последующей загрузкой в аналитическую базу данных SQLite и выполнением аналитических SQL-запросов.

---

## Цель проекта

Основная цель проекта — продемонстрировать навыки:

* работы с различными форматами данных;
* разработки ETL-процессов на Python;
* очистки и валидации данных;
* логирования результатов обработки;
* загрузки данных в аналитическую базу;
* написания аналитических SQL-запросов;
* контейнеризации приложения с использованием Docker.

---

## Источники данных

| Файл          | Формат | Описание                 |
| ------------- | ------ | ------------------------ |
| customers.csv | CSV    | Информация о клиентах    |
| orders.json   | JSON   | Данные о заказах         |
| products.xlsx | XLSX   | Каталог товаров          |
| payments.csv  | CSV    | Платежные операции       |
| events.xml    | XML    | Пользовательские события |

---

## Архитектура решения

```text
Исходные данные
│
├── customers.csv
├── orders.json
├── products.xlsx
├── payments.csv
└── events.xml
        │
        ▼
Извлечение данных (Extract)
        │
        ▼
Очистка и преобразование (Transform)
        │
        ├── удаление дублей
        ├── обработка пропусков
        ├── проверка типов данных
        ├── обработка ошибочных значений
        └── логирование качества данных
        │
        ▼
Загрузка (Load)
        │
        ▼
SQLite (music_analytics.db)
        │
        ▼
SQL-аналитика
```

---

## Структура проекта

```text
music-analytics-etl-pipeline/
│
├── data/
│   ├── customers.csv
│   ├── orders.json
│   ├── products.xlsx
│   ├── payments.csv
│   └── events.xml
│
├── src/
│   ├── extract.py
│   ├── transform.py
│   ├── load.py
│   └── main.py
│
├── sql/
│   └── analytics.sql
│
├── logs/
│   └── data_quality.log
│
├── Dockerfile
├── .dockerignore
├── requirements.txt
├── music_analytics.db
└── README.md
```

---

## ETL-процесс

### Extract

Извлечение данных реализовано из нескольких источников:

* CSV
* JSON
* XLSX
* XML

### Transform

На этапе преобразования выполняется:

* удаление дубликатов;
* обработка пропусков;
* приведение типов данных;
* проверка корректности дат;
* проверка числовых значений;
* очистка некорректных записей;
* расчёт поля `order_amount`.

Примеры выявленных проблем качества данных:

* некорректная дата заказа (`2025-99-99`);
* ошибочное значение суммы платежа (`error_amount`);
* пропущенные значения в обязательных полях;
* дублирующиеся записи.

### Load

После очистки данные загружаются в SQLite-базу данных:

* customers
* orders
* products
* payments
* events

---

## Контроль качества данных

Во время выполнения ETL формируется журнал качества данных:

```text
logs/data_quality.log
```

Пример содержимого:

```text
customers: raw_rows=715, clean_rows=684, removed_rows=31
orders: raw_rows=1220, clean_rows=1113, removed_rows=107
products: raw_rows=610, clean_rows=581, removed_rows=29
payments: raw_rows=1012, clean_rows=695, removed_rows=317
events: raw_rows=1501, clean_rows=1450, removed_rows=51
```

---

## Локальный запуск

### Создание виртуального окружения

```bash
python -m venv venv
```

### Активация окружения

```powershell
.\venv\Scripts\Activate.ps1
```

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск ETL-конвейера

```bash
python src/main.py
```

Результат выполнения:

```text
Data loaded to music_analytics.db

customers: 684 rows
orders: 1113 rows
products: 581 rows
payments: 695 rows
events: 1450 rows
```
---

## Запуск через Docker

### Сборка образа

```bash
docker build -t music-etl .
```

### Запуск контейнера

```bash
docker run --rm music-etl
```

---

## Аналитические запросы

Файл:

```text
sql/analytics.sql
```

Реализованы следующие отчёты:

1. Топ-10 клиентов по сумме заказов.
2. Выручка по месяцам.
3. Самые популярные товары.
4. Последняя активность клиентов с наибольшей выручкой.
5. Клиенты без заказов.

---

## Используемые технологии

* Python 3.14
* Pandas
* SQLAlchemy
* SQLite
* OpenPyXL
* XML ElementTree
* Docker

---

## Результат

В рамках проекта реализован полный ETL-конвейер, включающий извлечение данных из различных источников, очистку и контроль качества данных, загрузку в аналитическую базу данных и выполнение аналитических SQL-запросов.

Проект демонстрирует навыки работы с Python, SQL, обработкой данных, построением ETL-процессов и контейнеризацией приложений.
